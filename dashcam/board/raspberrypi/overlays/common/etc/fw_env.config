# These addresses must match:
# * the U-Boot configuration (in uboot.fragment)
# * the size/offsets in the genimage config file when we lay out the full disk image
# * the start offset in the RAUC system.conf file
/dev/mmcblk0  0x4000  0x8000
/dev/mmcblk0  0xC000  0x8000
